import HighCharts from "highcharts";

// special user id to use as identifier if "average" is selected as user to be displayed
export const averageFakeUserId = "Average";

// define colors to use in plots for different users
export const firstUserPlotColor = HighCharts.getOptions().colors[0];
export const secondUserPlotColor = HighCharts.getOptions().colors[3];
export const blackPlotColor = HighCharts.getOptions().colors[1];
